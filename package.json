{
  "name": "advga",
  "version": "1.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:android": "CAPACITOR=1 vite build && npx cap sync android",
    "apk:debug": "npm run build:android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "vite": "latest"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@trystero-p2p/mqtt": "^0.25.3",
    "trystero": "^0.25.3"
  }
}
